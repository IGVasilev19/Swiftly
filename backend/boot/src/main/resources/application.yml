server:
  port: 8080

spring:
  flyway:
    enabled: true
    locations: classpath:db-persistence.migrations
    baseline-on-migrate: false
    baseline-version: 1
  config:
    import: "classpath:application-persistence.properties"

  doc:
    default-produces-media-type: application/json
